import { MenuopenDirective } from './menuopen.directive';

describe('MenuopenDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuopenDirective();
    expect(directive).toBeTruthy();
  });
});
